@* @model List<StudentApp.Models.Student>

<h2>Nhập điểm cho khóa học</h2>

<form method="post" action="/Teacher/SaveGrades">
    <input type="hidden" name="CourseId" value="@ViewBag.CourseId ?? 0" />


    <table class="table">
        <thead>
            <tr>
                <th>Mã sinh viên</th>
                <th>Tên sinh viên</th>
                <th>Điểm số</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Count; i++)
            {
                <tr>
                    <td>@Model[i].Id</td>
                    <td>@Model[i].Name</td>
                    <td>
                        <input type="hidden" name="Students[@i].Id" value="@Model[i].Id" />
                        <input type="number" step="0.1" min="0" max="10" name="Students[@i].Score" class="form-control" />
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button type="submit" class="btn btn-success">Lưu điểm</button>
</form> *@
